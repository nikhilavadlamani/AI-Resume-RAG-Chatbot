[project]
name = "ai-resume-rag-chatbot"
version = "0.1.0"
description = "Production-style AI resume RAG chatbot"
readme = "README.md"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
